# Environment — NEVER commit secrets
.env
.env.local
.env.*.local

# Firebase / GCP service account keys — rotate immediately if committed
*.json
!package.json
!package-lock.json

# Build artifacts
main
bootstrap
bootstrap-*
*.exe

# Go toolchain
vendor/

# gomobile AAR
p2pmobile/*.aar

# Editor / OS
.DS_Store
.idea/
.vscode/
.cursor/
*.swp
