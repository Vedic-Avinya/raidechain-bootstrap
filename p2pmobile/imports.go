package p2pmobile

// Blank import required so that `go mod tidy` keeps golang.org/x/mobile
// in the dependency graph. gomobile bind / gobind needs this at build time.
import _ "golang.org/x/mobile/bind"
